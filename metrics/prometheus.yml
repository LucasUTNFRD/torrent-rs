global:
  scrape_interval: 10s

scrape_configs:
  - job_name: 'torrent-rs'
    static_configs:
      # Use host.docker.internal to access the metrics exporter on the host machine
      - targets: ['host.docker.internal:9000']
